export const metadata = {
    title: "Time From Now Calculator",
    description: "Determine the exact time from now by adding hours, minutes, and seconds. See the future time based on your input. Simple and accurate.",
};

export default function RootLayout({ children }) {
    return <>{children};</>;
}
