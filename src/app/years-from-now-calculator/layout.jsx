export const metadata = {
    title: "Years From Now Calculator",
    description: "Discover exact dates years from now. Input years and get the future date, day, week, and day count. Perfect for planning ahead.",
};

export default function RootLayout({ children }) {
    return <>{children};</>;
}
