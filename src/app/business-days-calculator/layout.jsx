export const metadata = {
    title: "Business Days Calculator",
    description: "Use our Business Days Calculator to determine the number of work days between two dates. Exclude weekends and holidays for accurate results.",
};

export default function RootLayout({ children }) {
    return <>{children};</>;
}
