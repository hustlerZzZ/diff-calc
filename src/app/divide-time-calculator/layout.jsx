export const metadata = {
    title: "Divide Time Calculator",
    description: "Divide any duration into equal segments. Input total time, specify divisions, and get each segment's duration. Ideal for splitting tasks.",
};

export default function RootLayout({ children }) {
    return <>{children};</>;
}
