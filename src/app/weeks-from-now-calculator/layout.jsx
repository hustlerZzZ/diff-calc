export const metadata = {
    title: "Weeks From Now Calculator",
    description: "Discover how many weeks lie between dates with the Weeks From Now Calculator. Input today and find out the distance in weeks – tailored just for you!",
};

export default function RootLayout({ children }) {
    return <>{children};</>;
}
