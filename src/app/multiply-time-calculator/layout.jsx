export const metadata = {
    title: "Multiply Time Calculator",
    description: "Calculate total time by multiplying durations. Input hours, minutes, or seconds and get the cumulative time. Ideal for repetitive tasks.",
};

export default function RootLayout({ children }) {
    return <>{children};</>;
}
