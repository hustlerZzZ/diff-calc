export const metadata = {
    title: "Add Time Calculator",
    description:
        "Calculate the average time of your activities. Input durations and get the mean time spent. Perfect for tracking daily tasks.",
};

export default function RootLayout({ children }) {
    return <>{children};</>;
}
