export const metadata = {
    title: "Months From Now Calculator",
    description: "Digital tool to calculate future dates based on months input. Find out exact dates months from today with ease.",
};

export default function RootLayout({ children }) {
    return <>{children};</>;
}
